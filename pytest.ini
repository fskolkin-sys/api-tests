[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -q
markers =
    e2e: end-to-end tests (may be slow and call external services)

log_cli = true
log_cli_level = INFO
